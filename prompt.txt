Design a web application with bootstrap on selecting the level it should get all the that 
level programs from a json file as a ordered list.list item should contains name of 
the program and two buttons on the right side of the page  one is desciption and other 
is full code. when  description button is clicked it should bring the description from 
the json in the list item only. when code button is clicked it should show the code 
from json file in the same list item.

generate json file with name as file name level as dvanced description must take from file between multiline comments /* */ and code must be take form the file only

git add .
git commit -m "Initial commit for web project"
git push origin web-fs-programs